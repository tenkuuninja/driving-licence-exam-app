import React from 'react';

const Footer = function() {
  return(
    <React.Fragment>
      <div className={`container`} style={{ fontSize: '3rem', textAlign: 'center' }}>
        Day la footer
      </div>
    </React.Fragment>
  );
}

export default Footer;
